{
  "name": "image-upload",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev --prefix client\" \"node server/index.js\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "node server/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
